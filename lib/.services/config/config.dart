//  Api configuration if you need to use one in your app.
const baseUrl = 'https://mydomain.com/';
const registrationApi = "${baseUrl}register";
const loginAPI = '${baseUrl}login';
const farmerAPI = '${baseUrl}farmer/';
const availableProduct = '${farmerAPI}available-products';
const soldProduct = '${farmerAPI}sold-products';
const deleteProduct = '${farmerAPI}delete-product';
const addProduct = '${farmerAPI}add-product';
const confirmRequest = '${farmerAPI}confirm-request';
